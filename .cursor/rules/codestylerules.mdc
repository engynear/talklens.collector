---
description: Эти правила кодстайла помогают поддерживать единообразие в коде C#/ASP.NET. Они соответствуют рекомендациям Microsoft, удобочитаемости кода и best practices
globs: 
alwaysApply: false
---
# 📌 Общие правила кодстайла для C#/ASP.NET  

## 🔹 Общие принципы  
- Код должен быть **читабельным и поддерживаемым**.  
- Соблюдать соглашения **Microsoft C# Coding Conventions**.  
- Использовать **CamelCase** для локальных переменных и параметров.  
- Использовать **PascalCase** для названий классов, методов и свойств.  
- Константы именовать в **PascalCase** (НЕ использовать `ALL_CAPS`).  
- Избегать магических чисел – использовать именованные константы или `readonly` поля.  

## 🔹 Неймспейсы  
- **НЕ** заключать код в фигурные скобки `{}` после `namespace`.  
✅ **Так правильно:**  
namespace MyApp.Services;
public class UserService { ... }

❌ **Так неправильно:**
namespace MyApp.Services
{
    public class UserService { ... }
}

 Операторы управления потоком
Если блок if содержит ОДНО действие – фигурные скобки {} можно не писать.
✅ Так правильно:
if (user == null) return;

❌ Так неправильно:
if (user == null) 
{ 
    return; 
}

Если if содержит несколько действий – фигурные скобки {} обязательны.
🔹 Классы и методы
Одна строка не должна превышать 120 символов.
Поля должны быть private и начинаться с _ (_fieldName).
Использовать var, если тип очевиден, иначе указывать явно

✅ Так правильно:
var users = GetUsers();
int count = users.Count;

❌ Так неправильно:
List<User> users = GetUsers();
var count = users.Count;
🔹 Константы и readonly
Константы (const) должны быть только для неизменяемых значений.
Использовать readonly, если значение задается в конструкторе.

✅ Пример:
public class Settings
{
    public const string AppName = "MyApp";
    private readonly int _maxUsers;

    public Settings(int maxUsers)
    {
        _maxUsers = maxUsers;
    }
}

🔹 Асинхронные методы
Использовать асинхронные методы с Task вместо void.
Имена асинхронных методов должны заканчиваться на Async.

✅ Пример:
public async Task<User> GetUserAsync(int id) { ... }
🔹 Использование using
Использовать using для работы с ресурсами (например, HttpClient).

✅ Пример:
using var client = new HttpClient();
🔹 Работа с LINQ
Если LINQ-запрос короткий, писать в одну строку.
Если длинный – разбивать на несколько строк.

✅ Пример:
var activeUsers = users.Where(u => u.IsActive).ToList();

❌ Так неправильно (если запрос длинный):
var activeUsers = users.Where(u => u.IsActive && u.LastLogin > DateTime.UtcNow.AddMonths(-1)).ToList();
✅ Лучше разбить:

var activeUsers = users
    .Where(u => u.IsActive)
    .Where(u => u.LastLogin > DateTime.UtcNow.AddMonths(-1))
    .ToList();
🔹 Заключение
Эти правила помогают AI в Cursor генерировать понятный, удобный и поддерживаемый код.
Важно: AI должен предлагать рефакторинг кода, если он не соответствует этим правилам.
